Agenda- Chapter(s)
*****************
Today's Agenda : chap21, chap22, chap23, Chap24

Chap21:  Object-Oriented Programming
************************************
	* A class can have
		- Instance attribute/variables: which are declared inside constructor with self. name is still an instance attribute, even though the assignment appears inside the class definition.
		- Instance Methods [ Need to add self as parameter ] It can access all the instance variables inside the method.
		- Class variables : A class variable belongs to the class, rather than being unique to each object. Declared inside the class, not initialized with Self. Each instance of a class, can access it , its common/shared to all the objects.In Python it is considered as static variable.
		- Class methods : [ Need to add cls as parameter ] It can access class variable.
		- Static method: It is declared with no self or cls. It belongs to the class , can consider it as utility function. Yes — a @staticmethod can access a class variable, but not automatically through self or cls. Through class name only.
		- Self means current object and cls means current class. 
		
	* Encapsulation 
		- Combining data and the methods that operate on that data together, and control how the data is accessed.
		- Private access modifier: Python doesnt have private access modifier. It uses name mangaling.
		  * for declaring private: __name
		    for declaring protected: _name
		- Getter and setter utility
		- @Property: @property in Python is a decorator that lets you access a method like an attribute.
			  @property
				def age(self):
					return self.__age

				@age.setter
				def age(self, value):
				
		** we can delete any of property using del <Class_Name>.<Property_Name>
				
	* Inheritence
	* Polymorphism
		- Python supports Static and dynamic binding. but Python does not generally perform Java-style compile-time method binding here. The actual method lookup happens at runtime. In Java, private, static and final, method bind happens at compile time as these belongs to class or it cannot be overridden. so Its gets binded at compile time only. In Python too, if we declare method as @staticmethod property then it will be binded at compile time. It only means the method doesn't receive self or cls.
		- super() allows the child class to access the parent implementation.
		- super() With __init__ : We can call super class constructor too and initialization depends the way we pass the attribute for initialization.
		
		** Duck-typing : If it behaves like a duck, treat it like a duck.
			Dog and Robot class has both speak() method. Now speak() is common. Now we can create a method which takes object as parameter and with that, we can call its associated method.
				def make_speak(obj):
					obj.speak()
					
				make_speak(Dog())
				make_speak(Robot()) ## Python cares about the object's behavior, not necessarily its inheritance hierarchy.
				
	* Abstraction
		- Hide implementation details and expose only what the user needs.
		- Abstract Class: Python provides from abc import ABC, abstractmethod
				from abc import ABC, abstractmethod
				class Animal(ABC):
					@abstractmethod
					def sound(self):
						pass
		  We cannot instaniate abstract class. we can only instantiate class which implements such abstract method.
		  
		  Abstract class
				│
				├── ✅ Constructor
				├── ✅ Instance variables
				├── ✅ Class variables
				├── ✅ Normal methods
				├── ✅ Abstract methods
				├── ✅ Static methods
				├── ✅ Class methods
				├── ✅ Properties
				├── ✅ Abstract properties
				├── ✅ Dunder methods
				├── ✅ Nested classes
				└── ✅ Inheritance
				
	* Dunder method	
		- Dunder methods are a python mechanism for hooking into the language's built-in operations. just like toString(), equals() etc..
		
	* Composition vs Inheritence	
		- IS-A : Inheritence, has-A: composition.
		
Chap22: OOP — Core Principles
*****************************
	* Name Mangling - Name mangling is Python's mechanism for handling attributes/methods whose names start with two underscores (__).
	  In python, if we try to access private variable, it will give error like attributeError. What python does internally, it changes the name to 
		__salary
			↓
		_Employee__salary
		
		Now we can access it emp._Employee__salary . This is called Name mangling.
	  Purpose : Mainly to avoid accidental name collisions in inheritance, not to provide true security. 
	  *** Name mangling applies to methods too
	
	* Method Overloading - Python does not support traditional method overloading like Java. The key reason is that Python treats a function/method name as a single name in a class namespace. It doesn't maintain multiple versions of the same method based on parameter count/types like Java does.
		** Python doesn't support traditional method overloading because defining a method with the same name again replaces the previous definition in the class namespace. Since Python is dynamically typed, it instead commonly uses default arguments, *args, **kwargs, or runtime dispatch to achieve similar behavior.
	
Chap23. Advanced OOP
********************
	* MRO (Method resolution order), its like (<class '__main__.Employee1'>, <class '__main__.Employee'>, <class 'abc.ABC'>, <class 'object'>)
	  Why MRO gives Object at the end? Every normal Python class ultimately inherits from object. Think of object as the root/base class. Because object provides fundamental behavior that all Python objects have, such as methods related to: all dunder functionalities.
	  
	* Diamond problem
		       A
			  / \
			 B   C
			  \ /
			   D
	When D calls show(), should Python use B → A or C → A? Python solves this using MRO + C3 linearization. It follows linear approach, D → B → C → A → object
	* Deep meaning of Super() - super() means: Continue method lookup according to the MRO from the current class. super() does not have to be the first line of a method. It can be used at any point where you need to call the next implementation in the MRO.
	
	* Dunder methods : Methods with __<MethodName>__ are called dunder methods. They allow your objects to behave like built-in Python objects.
	  If __str__ isn't defined, Python can fall back to __repr__.
	  
	  **** def __add__(self, other):
             return Number(self.value + other.value)
		 
	    a = Number(10)
		b = Number(20)
		
		c = a + b Here python changes it to a.__add__(b) where a is self and b is other. Both object holds its instance variable value. This is called operator overloading using dunder methods.
		
	* Mixins - A mixin is a small class designed to provide reusable functionality.
			class LoggingMixin:
				def log(self, message):
					print(f"[LOG] {message}")

			class Employee(LoggingMixin):
				def work(self):
					self.log("Employee started working")
					
			emp = Employee()
			emp.work() Here emp is self and LoggingMixin is inherited in Employee so Employee's object can access log method.
			
	* Dataclasses - Python provides dataclasses to reduce boilerplate for data-oriented classes. (More like Lombok in Java)
	
		from dataclasses import dataclass

		@dataclass
		class Employee:
			name: str
			age: int
			salary: float=2000
			
		Now emp = Employee("Manjeet", 30, 80000)
			print(emp)
			
		We dont need initialization and few of dunder methods like __str__/__repr__/__eq__
		
		** we can set default value too while defining dataclass like we did for salary default value as 2000.
		** Frozen - We can turn instance immutable. @dataclass(frozen=true) .This is useful when you want objects whose state shouldn't change after creation.
			
Chap24. Magic / Dunder Methods
******************************
	* We normally don't call magic methods directly. Python calls them automatically when you use certain syntax.
	** Dunder methods are not "extra methods" that Python programmers randomly created. They are hooks into Python's object model.
	* Various built-in methods
		* __init__ : For creating and initializing object. But How does internally works...
			__init__() initializes an object. It is not technically the method that creates the object. Object creation is associated with __new__().
			
			 def __new__(cls, name, age):
				print("Object created")
				return super().__new__(cls)
			
		* __str__() — Human-readable object representation
			When we do emp=Employee() print(emp) it gives <__main__.Employee object at 0x000001...> which is not readable. We need to include __str__ for having customized details.
			
		* __repr__() — Developer representation 
			Its similar to __str__, but more specific to dev debuging activity. If __str__() is not defined, Python can fall back to __repr__().
			For accessing it , we need to write like print(repr(s))
			
		* __len__() — len(object)
			This is equivalent to len() methods
			
		* __add__() — + operator
			This is equivalent to c=a+b [+ operator overloading ]
			
		* Arithematic magic operations
			| Operator | Magic method     |
			| -------- | ---------------- |
			| `a + b`  | `__add__()`      |
			| `a - b`  | `__sub__()`      |
			| `a * b`  | `__mul__()`      |
			| `a / b`  | `__truediv__()`  |
			| `a // b` | `__floordiv__()` |
			| `a % b`  | `__mod__()`      |
			| `a ** b` | `__pow__()`      |
			| `-a`     | `__neg__()`      |
			| `+a`     | `__pos__()`      |
			
		* Comparision Method Operation
			| Operator | Magic method |
			| -------- | ------------ |
			| `a == b` | `__eq__()`   |
			| `a != b` | `__ne__()`   |
			| `a < b`  | `__lt__()`   |
			| `a <= b` | `__le__()`   |
			| `a > b`  | `__gt__()`   |
			| `a >= b` | `__ge__()`   |
			
		* __getitem__() — object[index]
			This is for getting value based on Index
			
		* __setitem__() — object[index] = value
			This is for setting value based on index.
			
		* __contains__() — in
		
		* __iter__() — Iteration
			An iterator can implement: __iter__() and __next__()
			
			class Counter:
				def __init__(self, max_value):
					self.current = 0
					self.max_value = max_value

				def __iter__(self):
					return self

				def __next__(self):

					if self.current >= self.max_value:
						raise StopIteration

					self.current += 1
					return self.current
			Internally Sequence would be..
				iter(counter)
					 ↓
				__iter__()
					 ↓
				__next__()
					 ↓
				1
					 ↓
				__next__()
					 ↓
				2
					 ↓
				__next__()
					 ↓
				3
					 ↓
				StopIteration
				
		* __call__() — Make an object callable
			Suppose emp=Employee(), if we want to make object callable like emp(), we need to add __call__ dunder mehtod within class.
			
		* __bool__() — Boolean value
		
		* __enter__() and __exit__() — with [ This is being used as context manager ]
				with
				 ↓
				__enter__()
				 ↓
				body executes
				 ↓
				__exit__()

		* __slots__ — Special class attribute
			__slots__ = ["name", "age"] # __slots__ can restrict which instance attributes can be created and can avoid the normal per-instance __dict__ in suitable cases.
			s = Student("Manjeet", 45)
			s.city = "Delhi" # It will fail as city is not in the slot.
			
		* Attribute access magic methods
			__getattr__()
			__getattribute__()
			__setattr__()
			__delattr__()
		
		* Normal vs Magic method
			Normal method
				↓
			You call it explicitly

			Magic method
				↓
			Python syntax/operation triggers it