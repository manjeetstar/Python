Agenda- Chapter(s)
*****************
Today's Agenda : Chap2, Chap3, chap4, chap5, chap6, chap7, chap8

Chap2: Variable, datatype
*************************
	* Python determines the variable type at runtime.
	* list uses [], tuple uses (), set and dict uses {} and NoneType as none.
	* Python supports complex numbers like 3+4j Here 3 is real and j is imaginary.
	* List, set & dict is Mutable and Tuple, int, float, str, bool is immutable.
	* None represents the absence of a value.
	* type method is being used to knowing the type of variable.
	** Why input() Needs Type Conversion? - Input always returns a string value. To consider it in different type, we need to change its type. Without conversion, it will consider it as string and string concatenation will happen.
	* Multiple assignment : name, age, city = "Manjeet", 30, "Kolkata". We can assign the same value a = b = c = 0
	* Python doesnt have constant variable. for convention, Constant variable are declared in capital letter. Its just a convention.
	
Chap3: Operators
****************
	* An operator is a symbol or keyword used to perform an operation on values or variables.
	* Membership Operators - Used to check whether a value exists inside a sequence such as a string, list, tuple, etc.
		[in] and [not in]
	* Identity Operator - Used to check whether two variables refer to the same object.
		[is] and [is not] [ In python , like JS, there is no ===, it has only == for checking equality/value comparision ]
	* Bitwise Operation - &(And), |(Or) , ^(XOR), ~(Bitwise Not), <<(left shift) and >>(right shift)
	* Unary Operator - An operator works on a single value.
	
	** Operator precedence - 
			()
			**
			+x, -x, ~x
			*, /, //, %
			+, -
			<<, >>
			
Chap4: Input & Output
*********************, 
	* print bydefault at the end, goes to the new line and seperate input with space.
	      print("Manjeet", 20, "Canada", sep=" | ", end =" ")  It will Output like Manjeet | 20 | Canada and next print statement will append within the same line itself.
    * In print statement, we can use escape statements as well like \n, \t or for double quote like (\) etc
	* input() Always Returns a String . if we require difference type, we can cast it accordingly.
	* f-string : for formating string inside of print. No use of $, simply enclosed in {}
	
Chap5: Conditional Statements
*****************************
	* if ... elif ... else [Indentation is required ]
	
Chap6: Loops
************
	* This is basically being used mainly
			for i in range(start, end, step)
			With for loop, else statement too, we can add for like mentioning Loop completed.

chap7: Strings
**************
	* Triple quotes(""") are being used for multi-line strings.
	* Slicing - sequence[start : stop : step]
		Start tells where to begin; stop tells where to stop; step tells which direction to move.		
		If start is missing
			Step is positive → start from the beginning.
			Step is negative → start from the end.
		If stop is missing
			Step is positive → go until the end.
			Step is negative → go until the beginning.
		If both start and stop are missing
			Step is positive → take the sequence from beginning to end.
			Step is negative → take the sequence from end to beginning.
			
		Start is negative
			start is counted from the end.
			With positive step, movement is still left → right.
			With negative step, movement is still right → left.
		Stop is negative
			stop is counted from the end.
			With positive step, you move toward the right, stopping before that negative position.
			With negative step, you move toward the left, stopping before that negative position.
			
		If step is missing, Python always uses +1.
		
	** As we know strings are immutable, we cannot modify the exisiting string. Instead we create a new string.
	
	* Repeating Strings - Use * means print("Manjeet" * 4) op will be ManjeetManjeetManjeetManjeet 		
	* for search, we can use find() method. It Returns the index of the first occurrence.
	
	** String Formatting with Numbers
			price = 1234.5678
			print(f"Price: {price:.2f}") Here : is more important like you want to show numbers like 1,00,000

Chap8: Lists
************
	* A list is an ordered collection of lists. A list can contain different types of values.
	* Its Mutable in nature.
	* various methods are allowed...
		* extend: Append a list to a list. 
		* pop : Removes an item by index and returns that item.
		* sort : numbers.sort(reverse=True) // It changes the original list and its permanent until the scope is there.
		* sorted : Creates and returns a new sorted list.
	* Nested List - A list can contain an array of list too...
	* List concatenation - we can add 2 list by using + operator.
	* copy - For creating a new copy of list.
	* Unpacking a List - a, b, c = numbers [where number=[10,20,30]]
	  we can collect multiple list items in a variable using *
	  like a, *b = number //Now a=10, b=[20,30]
	* Overall Summary -
		1. List uses [ ]
		2. List is ordered
		3. Index starts from 0
		4. Negative indexing starts from -1
		5. List supports slicing
		6. List is mutable
		7. append() adds one item
		8. extend() adds multiple items
		9. insert() adds at a specific index
		10. remove() removes by value
		11. pop() removes by index
		12. sort() modifies the list
		13. sorted() returns a new sorted list
		14. reverse() modifies the list
		15. copy() creates a separate shallow copy
		16. List can contain another list
		17. List comprehension creates lists compactly
				