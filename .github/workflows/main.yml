name: For checking expression condition
on:
    push:
        branches: [ main ]
jobs:
    merge1:
        outputs:
            merge_status: ${{steps.abc.outputs.test_status}}
        if: github.ref == 'refs/heads/main'
        runs-on: [ ubuntu-latest ]
        steps:
            - name: Checking out the artifacts
              uses: actions/checkout@v4
              with:
                  ref: main
            - name: Building the app
              run: echo "Building the app"
            - name: Outputing the Job result
              id: abc
              run: |
                    echo "test_status=true" >> $GITHUB_OUTPUT    
    test:
        needs: [ merge1 ]
        if: needs.merge1.outputs.merge_status == 'true'
        runs-on: ubuntu-latest
        steps:
            - name: Test step
              run: echo "Testing the app"
    deploy:
        needs: [ merge1, test ]
        runs-on: [ ubuntu-latest ]
        steps:
            - name : Deploying the app
              run: echo 'Deploying the app in Prod'  
